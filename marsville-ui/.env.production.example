# Copy this file to .env.production and fill in the values before building for production.
# In CI/CD (GitHub Actions) these are set as repository secrets / env vars, not committed files.

# Full origin of the Azure App Service backend (no trailing slash).
VITE_API_BASE_URL=https://marsville-backend.azurewebsites.net
